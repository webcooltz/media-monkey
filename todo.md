also please make it so it only updates existing data if the user presses "refresh" in the kebab menu. also add that option to the kebab menu for each folder...

but new data is added still. just existing data remains there

way to look up images in a library somewhere

local mongodb or other DB for ease, not JSON

.env file

docker possibly, with mongodb, node, and react

also when it detects no (year), go into that folder to find the children movies in there

add "seasons"

add a way to view the show page, seasons page, etc.

compare data to IMDB or another DB

add subtitle finder/importer

integrate cleanvid